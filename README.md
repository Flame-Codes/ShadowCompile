# ShadowCompile
ShadowCompile is a lightweight Python bytecode compiler and Python obfuscation tool that converts ".py" scripts into raw compiled bytecode with randomized filenames. Perfect for Python bytecode generation, ".py" to ".pyc" conversion, lightweight source protection, script packaging, and Python code obscurity experiments.
